import { Publication } from '../types/Publication';

// No external publications added yet. This keeps the
// page structure intact while avoiding any references
// to the original template author's work.

export const publications: Publication[] = [];

export default publications;
